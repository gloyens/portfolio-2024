import { styled } from "@phntms/css-components";

import css from "./styles.module.scss";

export const DesktopIconsWrapper = styled("ul", {
  css: css.DesktopIconsWrapper,
});

export const Icon = styled("li", {
  css: css.Icon,
});
